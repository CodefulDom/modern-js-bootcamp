const convertingTemp = (tempF) => {
  return convertedValue = (tempF - 32) * 5 / 9;
}

const celsius = convertingTemp(68);
console.log(`Your converted temp from fahrenheit to celsius is, ${celsius} degree celsius`)